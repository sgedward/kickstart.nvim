#!/usr/bin/env bash
# Block commits that contain secrets/API keys.
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "pre-commit: gitleaks not found in PATH; install it to enable secret scanning." >&2
  exit 1
fi

gitleaks protect --staged --redact --verbose
